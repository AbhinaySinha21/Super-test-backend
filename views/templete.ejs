<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="cont">
        <h1>GLA articles</h1>
    </div>
    <h1 style="margin: 40px"><a href="/article/new"><button type="button" class="btn btn-danger">Add new article</button></a></h1>
    <ul>
    <% article.forEach((dem)=>{ %>
        <li>
        
        <h2>title <%= dem.title %> </h2>
        <h2>markdown <%= dem.markdown %></h2>
        <h2>description <%= dem.description.slice(0,10) %></h2>
        <p><%= new Date().toISOString().slice(0, 10) %></p>
        <div class="main">
        <form action="article/<%=dem.title %>" method="get">
            <button type="button" class="btn btn-primary">open the article</button>
        </form>
        <form action="article/<%= dem.title %>?_method=DELETE" method="post">
            <button type="button" class="btn btn-primary">Delete</button>
        </form>
        <form action="article/<%= dem.title %>?_method=PATCH" method="post">
            <button type="button" class="btn btn-primary">Edit</button>
        </form>
    </div>

    </li>
    <% }); %>
</ul>
</body>
</html>